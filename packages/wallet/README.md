# @snort/wallet

Lightning wallet integration for Snort: LNDHub, Nostr Wallet Connect, Alby and WebLN behind one `LNWallet` interface, plus a `Zapper` for NIP-57 zaps.

```bash
bun add @snort/wallet
```

Usage is documented in [docs/packages/wallet.md](../../docs/packages/wallet.md).
