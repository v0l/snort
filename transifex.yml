git:
  filters:
    - filter_type: file
      file_format: STRUCTURED_JSON
      source_language: en
      source_file: 'src/lang.json'
      translation_files_expression: 'src/translations/<lang>.json'
  settings:
    pr_branch_name: 'tfx/<br_unique_id>'
